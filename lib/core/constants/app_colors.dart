import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Cores principais — tema escuro musical
  static const Color primary = Color(0xFF6C63FF); // Roxo vibrante
  static const Color accent = Color(0xFFFF6584); // Rosa coral
  static const Color background = Color(0xFF0F0F1A); // Preto azulado
  static const Color surface = Color(0xFF1E1E2E); // Superfície card
  static const Color onSurface = Color(0xFFE0E0E0); // Texto sobre card
  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFF9E9E9E);
  static const Color success = Color(0xFF4CAF50);
  static const Color error = Color(0xFFEF5350);
}
